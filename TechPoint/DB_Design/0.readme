  
   字段值来自业务控制 越复杂的业务对应的业务点就越多
	 是否将逻辑放到数据库 表-view-存储过程-触发器....
	 字段命名，组合的每个单词都应该对应单独单词不能组成名字的每个小元素由不同的单词组成  类命名

问题：
    1.uml图 数据库模型（业务 - 表关系的映射 ）( 表的字段值 )
    2.表设计中是靠多添加字段还是连表还是外键 
    3.数据库设计字段类型选择 long varchar String vachar ...对应类中的类型
    4.数据库主键设计 /是否为业务主键  主键生成策略
